import { Account, AuditLogs, Session, User } from '../types';

export const users: User[] = [];
export const accounts: Account[] = [];
export const sessions: Session[] = [];
export const auditLogs: AuditLogs[] = [];
